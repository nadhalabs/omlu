# Example only. Flutter reads these through --dart-define, not automatically.
OMLU_FRONTEND_URL=https://omlu.in
OMLU_ALLOWED_DOMAINS=omlu.in
OMLU_ALLOW_HTTP=false
# API and WebSocket base URL used by the native operations client.
OMLU_BACKEND_URL=https://omlu-api.onrender.com
